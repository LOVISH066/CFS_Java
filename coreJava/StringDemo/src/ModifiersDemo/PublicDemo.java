package ModifiersDemo;

public class PublicDemo {
    // in public we can acces it from other package as well any other class
    // but in protected we can only access from same package and in sub classes of same package
}
