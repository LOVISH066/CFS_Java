public class ChartsStr {
    public static void main(String[] args) { // this is method
        char arr[]={a,b,c,d,e,f}; // this is data memeber also these are in class so it is encapsultion like rember name with capsule
        String name = new String(arr);
        System.out.println(name);

        // here it is encapsulation

    }
}
